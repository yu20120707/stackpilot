# 故障分析信息补充清单

当飞书线程里已经出现“报警”“故障”“报错”“异常”但上下文不足时，优先补齐以下信息，再继续做判断：

- 告警开始时间、恢复时间、峰值时间
- 影响范围：受影响服务、接口、租户、用户量、错误率
- 关键错误日志：异常栈、错误码、trace_id、request_id
- 最近变更：发布、配置变更、开关变更、扩缩容、数据库变更
- 当前处置动作：回滚、重启、限流、熔断、降级
- 监控证据：QPS、RT、5xx、timeout、CPU、内存、连接池、下游依赖状态

如果以上信息缺失，不要给出强结论；应先明确写出缺少信息，并建议补充错误日志、最近变更记录和影响范围。
